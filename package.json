{
    "name": "create-mock-bun",
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "prepare": "npm run build",
        "test": "vitest"
    },
    "devDependencies": {
        "vitest": "^1.0.0",
        "typescript": "^5.0.0",
        "@types/node": "^20.0.0"
    },
    "dependencies": {
        "@types/bun": "^1.3.1"
    },
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": ["dist"]
}
}